# [OPEN] Debug Session: double-mark-drag

## Symptoms
- `单双` 在滑动刷选时仍然不能稳定显示。
- `DML` 点击修改存在不稳定或无效情况。
- `档位 / DML` 标注文字拖动手感差。

## Hypotheses
1. 同一次 `单双` 刷选内对同一线条发生重复 toggle，最终状态被抵消。
2. `单双` 状态已写入，但对应 text 节点未创建或未进入当前渲染集合。
3. `DML` 点击命中了 text 节点后，没有稳定转换为对应 line 的修改动作。
4. 文本拖拽阶段的命中/阈值/pointer-events 配置互相冲突，导致看似“难拖动”。

## Plan
1. 启动调试服务器并注入埋点。
2. 复现 `单双` 刷选、`DML` 点击、文字拖拽。
3. 基于日志确认真实失败点。
4. 再做最小修复并对比修复前后日志。
